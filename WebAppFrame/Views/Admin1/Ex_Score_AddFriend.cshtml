
<!DOCTYPE html>

<html>
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <title></title>
    <link href="/Styles/style2.css?id=0" rel="stylesheet" />
    <script src="~/Scripts/jquery.min.js"></script>
    <script src="~/Scripts/miniui/boot.js" type="text/javascript"></script>
    <script src="~/Scripts/Global.js"></script>

    <link href="~/Styles/public.css?id=1" rel="stylesheet" />
    <script src="~/Scripts/public.js?id=5"></script>

    <style>
        select {
            width: 80px;
            border-radius: 5px;
            border: 1px solid #dfdfdf;
        }

        .sys-summary {
            display: inline-block;
            text-align: center;
            min-width: 20px;
            font-size: 18px;
            color: #ff6a00;
        }

        .photo-img {
            width: 40px;
            height: 40px;
            border: solid 1px #dcdcdc;
            border-radius: 50%;
        }

        .photo-img-big {
            width: 100px;
            height: 100px;
        }

        .red {
            font-size: 20px;
            font-weight: bold;
            color: red;
            height: 38px;
            line-height: 38px;
        }

        .blue {
            font-size: 18px;
            font-weight: bold;
            color: blue;
            height: 38px;
            line-height: 38px;
        }

        .yellow {
            font-size: 18px;
            font-weight: bold;
            color: yellow;
        }

        .mny {
            display: inline-block;
            min-width: 100px;
            height: 40px;
            line-height: 40px;
            font-size: 18px;
            font-weight: bold;
            text-align: center;
            margin-left: 3px;
        }
    </style>

    <script type="text/javascript">
        var grid;
        $(function () {
            //设置表格的高度
            var parentHeight = $(window.parent.document).find("#rightFrame").height();
            $("#datagrid1").height(parentHeight - 65);

            mini.parse();
            grid = mini.get("datagrid1");

            grid.load();
            //search();

            drawCell();

            init();
        })

        function init() {

        }
        var servUrl_AppData = '@ViewData["servUrl_AppData"]';
        function drawCell() {
            grid.on("drawcell", function (e) {
                var record = e.record,
                    column = e.column,
                    field = e.field,
                    value = e.value;
                //详情
                if (column.name == "dtl") {

                    e.cellStyle = "text-align:left";
                    e.cellHtml = '<a style="color:blue;" href="javascript:UserAllInfo(\'' + record.uid_to + '\')">&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;全貌</a>';
                }
                if (column.name == "photo_from") {
                    var photo = servUrl_AppData + '/photo/' + record.photo_from;

                    var nick = record.nick_from;
                    var sex = record.sex_from;
                    var color = "";
                    if (sex == "男生") {
                        color = "blue";
                    } else {
                        color = "red";
                    }

                    e.cellStyle = "text-align:center";
                    e.cellHtml = '<a href="javascript:UserAllInfo(\'' + record.uid_from + '\')"><img src="' + photo + '" class="photo-img" />';
                    e.cellHtml += "<br/>";
                    e.cellHtml += "<span style='color:" + color + "'>" + nick + "(" + sex + ")" + "</span></a>";
                }

                if (column.name == "photo_to") {
                    var photo = servUrl_AppData + '/photo/' + record.photo_to;

                    var nick = record.nick_to;
                    var sex = record.sex_to;
                    var color = "";
                    if (sex == "男生") {
                        color = "blue";
                    } else {
                        color = "red";
                    }

                    e.cellStyle = "text-align:center";
                    e.cellHtml = '<a href="javascript:UserAllInfo(\'' + record.uid_to + '\')"><img src="' + photo + '" class="photo-img" />';
                    e.cellHtml += "<br/>";
                    e.cellHtml += "<span style='color:" + color + "'>" + nick + "(" + sex + ")" + "</span></a>";
                }
                if (column.name == "c_add") {
                    var dtm = record.c_add;

                    e.cellHtml = '<span class="red">' + dtm + '</span>';
                }
                if (column.name == "c_sub") {
                    var dtm = record.c_sub;

                    e.cellHtml = '<span class="red">' + dtm + '</span>';
                }
                if (column.name == "c_diff") {
                    var dtm = record.c_diff;

                    e.cellHtml = '<span class="blue">' + dtm + '</span>';
                }
            });
        }

        function Add(id) {
            var parentHeight = $(window.parent.document).find("#rightFrame").height();
            var parentWidth = $(window.parent.document).find("#rightFrame").width();

            mini.open({
                url: "/Admin1/SysUser_Add/" + id,
                showMaxButton: false,
                title: "添加用户",
                width: 600,
                height: 400,
                ondestroy: function (action) {
                    if (action == "ok") {
                        grid.reload();
                    }
                    else if (action == "cancel") {

                    }
                }
            });
        }
    </script>


</head>
<body>
    <div class="place">
        <span>位置：</span>
        <ul class="placeul">
            <li><a href="#">好友积分</a></li>
        </ul>
    </div>
    <form id="form1">

        <div id="datagrid1" class="mini-datagrid" style="width:100%;height:600px;"
             url="/Admin1/GetExScoreAddFriendData" idfield="id" allowresize="true" multiselect="true"
             sizelist="[50,100,200]" pagesize="50">
            <div property="columns">
                <div field="xh" width="10" renderer="onGenderRenderer" align="center" headeralign="center">序号</div>
                <div field="uid_from" width="35" renderer="onGenderRenderer" align="left" headeralign="center">uid</div>
                <div name="photo_from" width="35" renderer="onGenderRenderer" align="left" headeralign="center">头像</div>
                <div field="name" name="" width="15" headeralign="center" align="left" allowsort="false">签约用户</div>
                <div name="photo_to" width="35" renderer="onGenderRenderer" align="left" headeralign="center">头像</div>
                <div field="d1" name="" width="35" headeralign="center" align="right" allowsort="false">d1</div>
                <div field="d2" name="" width="35" headeralign="center" align="right" allowsort="false">d2</div>
                <div field="c" name="" width="10" headeralign="center" align="right" allowsort="false">c</div>

                <div name="dtl" width="100" headeralign="center" align="center" allowsort="false">全貌</div>
            </div>
        </div>
    </form>
</body>
</html>
